# Production Environment Variables Example
# Copy this file and set these environment variables in your production environment
# DO NOT commit actual values to version control

# Database Configuration (REQUIRED)
APP_DB_HOST=your-db-host.example.com
APP_DB_PORT=5432
APP_DB_NAME=teamtempo_prod
APP_DB_USERNAME=teamtempo_prod_user
APP_DB_PASSWORD=your-secure-password-here

# JWT Security (REQUIRED - Generate a secure random key)
# Generate with: openssl rand -hex 64
JWT_SECRET_KEY=your-256-bit-secret-key-here-must-be-at-least-256-bits-long

# Server Configuration (Optional)
SERVER_PORT=8080
SERVER_ERROR_INCLUDE_STACKTRACE=never

# Database Connection Pool (Optional - defaults provided)
DB_POOL_MAX_SIZE=20
DB_POOL_MIN_IDLE=10
DB_POOL_CONNECTION_TIMEOUT=30000
DB_POOL_IDLE_TIMEOUT=600000
DB_POOL_MAX_LIFETIME=1800000

# Logging Configuration (Optional)
LOG_FILE=/var/log/teamtempo/application.log
# or
LOG_PATH=/var/log/teamtempo/



